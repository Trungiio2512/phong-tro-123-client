import React from "react";
import PropTypes from "prop-types";

const Sidebar = (props) => {
    return <div className="max-w-[256px] w-full h-full">Sidebar</div>;
};

Sidebar.propTypes = {};

export default Sidebar;
